@page
@model Serb_Diana_Proiect.Pages.Motoare.IndexModel

@{
    ViewData["Title"] = "Motorizari";
}

<h1>Motorizari</h1>

<p>
    <a asp-page="Create">Adauga</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Motor[0].TipMotor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Motor) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TipMotor)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Editeaza</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Detalii</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Sterge</a>
            </td>
        </tr>
}
    </tbody>
</table>
